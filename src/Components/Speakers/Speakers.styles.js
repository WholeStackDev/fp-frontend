import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(theme => ({
  root: {
    maxWidth: "50rem",
    margin: "auto"
  },
  table: {
    width: "100%"
  }
}));

export default styles;
